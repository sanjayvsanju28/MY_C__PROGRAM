#include <stdio.h>
#include<math.h>
int main(){
    float x1,x2,y1,y2,di;
    printf("enter the values of x1,y1\n");
    scanf("%f %f",&x1,&y1);
    printf("enter the value of x2,y2\n");
    scanf("%f %f",&x2,&y2);
    di=sqrt((x2-x1)*(x2-x1)-(y2-y1)*(y2-y1));
    printf("%f",di);
}
